# course-kernel

Shared composition contracts for the course enrolment example.

Both the application and vertical slice packages depend on this package. The
kernel does not depend on the application or on any concrete slice.
